[Unit]
Description=Update the weather lamp every hour

[Timer]
# 毎時0分。起動直後にも一度走らせる。
OnCalendar=*:00
OnBootSec=2min
# その時刻にラズパイが止まっていたら、起動後に実行する
Persistent=true
# 気象庁の発表直後に集中しないよう少しばらす
RandomizedDelaySec=60

[Install]
WantedBy=timers.target
